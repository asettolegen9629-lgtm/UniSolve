VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key_here
# Local dev (default in code is localhost:3000/api in DEV only)
VITE_API_URL=http://localhost:3000/api
# Vercel Production: must match your Render Web Service hostname exactly (typo = CORS “no header” errors).
# Example: https://unisolve.onrender.com/api — NOT unislove.onrender.com unless that hostname is your live service.
